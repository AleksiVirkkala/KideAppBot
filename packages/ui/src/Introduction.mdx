import { Meta } from '@storybook/blocks'
import Image from 'next/image'

<Meta title="Introduction" />

# KideAppBot Storybook

This library is a collection of React components developed with React + Tailwind + HeadlessUI. The components are used in the KideAppBot project.
